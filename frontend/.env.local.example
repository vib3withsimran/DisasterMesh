# DisasterMesh Frontend — Environment Variables
# Copy to .env.local and fill in values.

# Backend API URL (where the FastAPI server is running)
NEXT_PUBLIC_API_URL=http://localhost:8000

# MapLibre GL JS — NO API KEY NEEDED!
# MapLibre uses free open-source tile servers.
# No NEXT_PUBLIC_MAPBOX_TOKEN required.
